version: "3.7"

services:

    app:
        image: ghcr.io/minjja/docker-grav:master
        build:
            context: .
            dockerfile: docker/Dockerfile
        ports:
            - 80
        volumes:
            - type: bind
              source: ${PWD}
              target: /var/www/html
